<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Item</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <div class="container mt-5">
        <h1>Item Details Information</h1>
        <div class="card">
            <div class="card-body">
                <h3 class="card-title"><%= item.name %></h3>
                <h5 class="card-subtitle text-muted">Category: <%= item.category %></h5>
                <p class="card-text mt-3">Description: <%= item.description || "No description provided" %></p>
                <ul class="list-group list-group-flush">
                    <li class="list-group-item"><strong>Quantity:</strong> <%= item.quantity %></li>
                    <li class="list-group-item"><strong>Price:</strong> ₱<%= item.price.toFixed(2) %></li>
                    <li class="list-group-item"><strong>Added On:</strong> 
                        <% const date = new Date(item.createdAt); %>
                        <% const formattedDate = `${date.getMonth() + 1}/${date.getDate()}/${date.getFullYear()}`; %>
                        <%= formattedDate %>
                    </li>
                </ul>
                <div class="mt-3">
                    <a href="/items/<%= item._id %>/edit" class="btn btn-warning">Edit</a>
                    <form action="/items/<%= item._id %>?_method=DELETE" method="POST" style="display: inline;">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                    <a href="/items" class="btn btn-secondary">Back to List</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
